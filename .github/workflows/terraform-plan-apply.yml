name: terraform-plan-apply

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to run against"
        type: choice
        options:
          - dev
        default: dev
      command:
        description: "Terraform command"
        type: choice
        options:
          - plan
          - plan-apply
        default: plan

permissions:
  contents: read

jobs:
  terraform:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure login (service principal)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}

      - name: Ensure tfstate backend (idempotent)
        run: bash scripts/bootstrap-tfstate.sh
        env:
          TFSTATE_RG: ${{ vars.TFSTATE_RG }}
          TFSTATE_LOCATION: ${{ vars.TFSTATE_LOCATION }}
          TFSTATE_SA: ${{ vars.TFSTATE_SA }}
          TFSTATE_CONTAINER: ${{ vars.TFSTATE_CONTAINER }}
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Terraform orchestrator
        uses: JT-Engineering-Dev/terraform-orchestrator-action@v0.2.0
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
        with:
          working-directory: terraform
          workspace: ${{ github.event.inputs.environment }}
          var-file: environments/${{ github.event.inputs.environment }}.tfvars
          command: ${{ github.event.inputs.command }}
          backend-config-file: backend.config
